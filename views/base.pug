doctype html
html
  head
    title natours | #{tour}
    link(rel='stylesheet' href='css/style.css')
    link(rel='shortcut icon' type='image/png' href='img/favicon.png')

  body
    h1 natours
    //- buffered code example
    h2= tour.toUpperCase()
    //- non-buffered code example
    - const test = 2 * 4;
    h4= test
    //- interpolation example
    p a tour from #{user}